## 编程作业

首先上一章的三个系统调用迁移了过来，由于 `Task` 模块的结构稍有变化，所以稍微改了一点东西，比如获取当前任务控制块之后进行的操作不同了。

`sys_spawn` 的实现方式相当于直接把 `exec` 的实现方式拿过来了，只不过与它不同的是没有覆盖当前进程的地址空间，而是新建了一个进程，创建对应的进程控制块，改它的地址空间。

`stride` 按照要求模拟就好了，主要改的地方就是 `fetch` 取出一个进程控制块的时候，以前简单的时间片轮转是直接拿出第一个，现在要取出 `stride` 字段最小的一个。可以用优先队列，但是要改的东西太多了，我就简单的用了一个 `Vec`，每次遍历整个 `Vec` 来选取合适的进程。



## 简答作业

### Stride 算法深入

1. 不是。因为此时 pass 是 10，那么此时就会对 p2 的 stride 字段进行 250 + 10，而 u8 最大才 255，所以就会溢出，在 Rust 里 release 运行时发生溢出，260 就会变成 4，那这么来看 p2 的 stride 反而比 p1 小了，所以还是 p2 运行。
2. 因为更新 stride 的步长至少是 2，不会出现两个 stride “几乎重合”但从不同方向绕过一圈的情况。
3.  

```rust
impl PartialOrd for Stride {
    fn partial_cmp(&self, other: &Self) -> Option<Ordering> {
        let diff = self.0.wrapping_sub(other.0);
        // 如果差值小于等于一半，说明 self 比 other 大
        if diff == 0 {
            None
        } else if diff <= BIG_STRIDE / 2 {
            Some(Ordering::Greater)
        } else {
            Some(Ordering::Less)
        }
    }
}
```




## 荣誉准则

1. 在完成本次实验的过程（含此前学习的过程）中，我没有与他人进行交流。
2. 此外，没有参考其它资料。
3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。
5. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。